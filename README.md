
  # Location-based Memo App Prototype

  This is a code bundle for Location-based Memo App Prototype. The original project is available at https://www.figma.com/design/UHbEGhuWRaA274wknDE6PA/Location-based-Memo-App-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  